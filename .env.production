# Vite bakes this into the bundle at build time — set it before `npm run build`,
# App Service application settings cannot change it after the build.
VITE_API_BASE_URL=my-free-app-function-bmauhpd9h3fqfjcw.southafricanorth-01.azurewebsites.net/api
